# Pi Notes 设计

> 默认注入、`.note` 项目来源与关键词按需提醒已实现。使用方式见 [README](README.md)，验证范围见 [验证记录](docs/verification.md)。

## 目标

让 Pi 直接使用用户在 Obsidian 或其他编辑器中维护的 Markdown 笔记。用户指定一个目录，插件自动提供根笔记和子文件夹入口，AI 根据当前任务继续读取、浏览或搜索。

用户继续使用 Pi 原有对话和工具；笔记文件本身是共享的数据源。

## 默认提供什么

对配置目录的直接子项，提供两类内容：

- **Markdown 笔记**：始终附带文件名、绝对路径和已配置的定位，根据 frontmatter 决定展示全文或 description。
- **文件夹**：提供文件夹名和绝对路径，作为按需浏览、搜索的入口。

默认入口的发现范围是这一层。子目录内的笔记由 AI 进一步浏览、搜索，或通过下述关键词提醒发现；其中的 `defaultopen` 不会使笔记越过目录层级自动全文注入。

插件同时提供配置目录的绝对路径。发现范围为可见的普通 Markdown 文件和子文件夹，忽略点号开头的隐藏条目、符号链接条目及非 Markdown 文件。显式配置的根目录可以通过符号链接定位。这只是发现规则，不是文件访问权限隔离。

### 笔记属性

以用于用户建模的 `USER.md` 为例：

```yaml
---
description: 用户的协作偏好、表达习惯和长期目标。
purpose: >-
  这是对用户的持续建模，用于理解用户并辅助沟通与判断。
  结合当前情境使用，区分明确偏好与推断，当前明确要求优先。
defaultopen: true
---
```

默认展示的三个字段各自承担一个职责：

- `description`：笔记里有什么，以及何时值得读取。
- `purpose`（定位）：笔记对 AI 的价值、内容性质与使用方式，是用户编写的自由文本。
- `defaultopen`：控制正文是否默认展开。

`defaultopen: true` 时提供完整正文；`false` 时提供 description，正文留待按需读取。两种模式都会在路径之后提供已配置的定位。Frontmatter 用于装配这些内容，不重复输出整段属性。

缺省行为：未填写 `defaultopen` 时按 `false` 处理；缺少 description 时仍提供文件名和路径；未配置定位时省略该项。属性格式错误时明确报告对应文件，保留路径入口，不擅自切换为全文注入。

`USER.md` 使用同样的笔记机制，其作用由定位和正文表达。插件统一处理用户编写的定位；工具权限与指令优先级继续采用 Pi 原有规则。

### 默认上下文示意

以下路径和笔记仅用于说明结构：

```markdown
# 笔记

目录：/example/vault/AI
按需读取笔记全文、浏览子文件夹，或使用可用的搜索工具查找内容。

## 已展开笔记

### USER.md
路径：/example/vault/AI/USER.md
定位：这是对用户的持续建模，用于理解用户并辅助沟通与判断。结合当前情境使用，区分明确偏好与推断，当前明确要求优先。

用户偏好简洁、可快速扫读的回复，先给结论，细节按需展开。
用户希望 AI 基于证据独立判断，而不是以附和代替分析。

## 按需阅读

- 写作偏好.md
  路径：/example/vault/AI/写作偏好.md
  定位：作为撰写和修改文章时的风格指南，结合当前文章的读者与目的使用。
  描述：我的写作风格与措辞偏好，撰写文章时参考。

## 文件夹

- 项目：/example/vault/AI/项目
- 资料：/example/vault/AI/资料
```

## 如何继续深入

AI 根据已提供的路径，使用当前 Pi 会话中可用的工具：

- 读取某篇笔记的全文。
- 列出某个文件夹的内容，再选择下一层入口。
- 用文件搜索或内容搜索定位相关笔记；安装了 zg 等搜索工具的环境，也可以使用相应工具。

插件负责提供来源和可操作的路径，搜索能力沿用当前环境。笔记中的 Markdown 和 Obsidian 链接保留原文。

## 配置与刷新

全局笔记目录可以是整个 Obsidian 库，也可以是库内的一个文件夹。配置保存在 Pi agent 目录下的 `notes.json`，遵循 `PI_CODING_AGENT_DIR`。未配置 `directory` 时，若 agent 目录下存在 `notes/`，则使用该目录；`directory: null` 表示显式停用全局来源。配置后，日常提问无需手工附加笔记。

`/notes` 提供当前目录和注入概况，主要操作是查看只读预览，目录设置与停用是次要操作。沿用 Pi 原生选择器和配色；预览按终端高度分页，窄屏优先保留返回和滚动提示。正常对话仍从 Pi 原有输入框开始。

每次开始处理新的用户输入前检查根目录变化，更新本轮默认上下文。已经发出的模型请求不受随后文件修改影响。

## 默认注入的实现边界

使用 Pi 扩展的 `before_agent_start` 在现有 system prompt 后追加笔记上下文，保留原有提示和 AGENTS.md 内容。每轮从当前文件状态生成这一块内容，避免反复向会话历史追加整份笔记。

默认扫描只访问根目录的直接子项；按块读取根笔记的 frontmatter，全文模式再读取正文。以稳定顺序装配内容，按文件标识、大小和修改时间复用解析结果。缓存按序列化大小限制在注入预算的两倍，避免目录增长导致无界缓存。

笔记块默认预算为 256 KiB UTF-8 字节，可在配置中调整为 1 KiB 到 16 MiB；单篇 frontmatter 上限为 64 KiB。单篇无效或超限时保留路径与错误说明；整块超预算或目录不可用时，报告本轮笔记上下文不可用。正常注入始终提供完整选定内容。

首版插件负责配置、发现和注入，笔记读写与搜索沿用 Pi 工具及用户的任务授权。自动注入的笔记内容会随模型请求发送给当前模型提供方，用户应选择适合共享的目录。

## 项目笔记目录（.note）

Pi 在项目内工作时自动使用项目内的 `.note` 目录，无需逐项目全局配置。全局目录与项目来源并存：全局配置仍是第一来源，项目来源补充在后面。

- **发现**：从会话工作目录向上到 git 根（无 git 时到文件系统根）的每个目录中，查找名为 `.note` 的直接子目录。每轮处理新提示前重新发现，复用现有缓存机制；发现范围内不再嵌套查找 `.note`。相同来源根路径去重；位于全局目录内部的 `.note` 仍单独参与，因为全局扫描会跳过隐藏目录。
- **信任门槛**：仅当当前项目已受 Pi 信任时注入该来源；未信任时在预览中列出来源与原因，不注入内容。与 `.pi/extensions`、`.pi/skills` 的信任边界一致，防止第三方仓库内容自动进入模型上下文。Pi 对不含 trust-requiring 资源（如 `.pi` 目录）的目录默认视为受信任，`.note` 跟随该判定，与 AGENTS.md 等上下文文件同层。
- **内容规则**：`.note` 内的直接子项按现有根目录规则处理，frontmatter、预算与错误报告不变；来源在注入块中各自带标题与绝对路径，同名笔记靠完整路径区分，不做特殊文件名处理。
- **预算**：来源按全局在前、项目由浅到深有序累积；超出预算时整份来源排除并明确报告，不部分截断笔记、不静默丢弃。
- **入口**：`/notes preview` 列出全部来源及信任状态；不新增用户必经操作，日常使用仍然是在项目里直接打开 Pi。

## 关键词按需提醒

根据对话中的关键词提供相关笔记入口，AI 判断是否需要继续读取。用户继续从 Pi 原有输入框工作，只需在希望参与提醒的笔记中填写 `keywords`。

### 属性与发现范围

```yaml
---
keywords: [字体, font-family, 排版]
description: Web 项目的统一字体栈与使用约定。
purpose: 设置或调整 Web 字体时参考。
---
```

- `keywords` 是可选字符串列表，元素去除首尾空白后须非空；未填写或空列表时不参与匹配。按字面短语匹配，任一关键词命中即可，英文大小写不敏感。
- 索引覆盖本轮可用的全局笔记目录，以及按现有发现规则选入、通过信任门槛的 `.note` 来源，递归包含各来源的子目录；整份来源被预算或读取错误排除时，其深层笔记也不参与本轮提醒。来源内部沿用普通 Markdown 文件、隐藏条目和符号链接的发现规则；`.note` 本身作为明确选入的来源处理。
- 每个来源的根层笔记继续按原规则默认展示；已在上下文提供的根层笔记不再通过关键词重复注入。子目录中的 `defaultopen` 不改变关键词提醒的摘要形式。

### 触发时机与提醒内容

匹配对象是新出现的用户输入、模型文本回复，以及模型渠道实际提供给插件的思考文本。工具调用参数、工具结果和插件注入的笔记内容不作为触发输入。流式文本跨片段的关键词按连续文本识别。

用户输入中的命中在本次模型请求前提供；模型思考或回复中的命中留到下一次正常模型调用前提供，包括同一任务的工具调用后续轮次。最终回复后没有正常调用时，保留待提醒状态，等待后续正常调用。正在发送或生成的请求保持原样。

一次提醒只包含笔记名、绝对路径、已配置的 `purpose` 和 `description`；缺失的可选字段省略，全文仍由 AI 使用现有工具按需读取。提醒使用已有元数据，无需模型生成摘要，也不触发额外模型调用。

### 按当前上下文去重

以笔记路径区分身份，同名但不同路径的笔记分别处理。默认注入内容和关键词提醒共同参与去重；同一笔记命中多个关键词、或在不同触发对象中反复命中，都只保留一份提醒。

去重依据是当前请求实际保留的笔记内容，不是会话曾经注入过的永久名单。默认清单中的根层条目已算提供，即使没有 `description`。关键词提醒携带可识别的笔记身份，并随对应消息保留；其他消息中仅提及路径或目录不算提供该笔记。后续请求复用仍在上下文中的提醒，不再追加副本。

会话恢复、分支切换和上下文压缩后，重新与当前分支的有效上下文对账。仍保留的笔记继续去重；已移出上下文的笔记在新的关键词命中时可以再次提醒。待提醒状态按会话和分支隔离，尚未实际提供的笔记保持待提醒。

实现使用带笔记身份的隐藏自定义消息保存提醒；`context` 钩子装配本次请求，`sendMessage` 以 `triggerTurn: false` 在宿主安全边界保存消息。当前工具循环保留轻量内存副本，直至 `agent_end`，覆盖宿主运行中消息快照的差异。

分支内以小型命中/完成增量保存待提醒路径。每条原始用户或模型消息都记录来源 ID 检查点；恢复时只补处理末尾未检查的原始消息。发送队列中的提醒保持待确认，核验对应投递 ID 已成为真实会话消息后再完成，支持落盘前中断恢复。恢复时按时间顺序同时对账已持久化的提醒，覆盖消息已落盘但完成记录尚未写入的边界，避免压缩后凭旧命中重现。

### 加载与资源边界

索引只读取发现和匹配需要的文件信息与 frontmatter。`KeywordStore` 按来源管理缓存与文件监听：会话打开时后台加载持久缓存并校验文件签名，平时按变化路径增量更新；未变化的发送直接复用已发布索引。文件签名包含设备、文件标识、大小和纳秒级修改/状态时间。根目录被替换时重新校验来源。

持久缓存位于 agent 目录的 `cache/pi-notes/`，按来源路径生成文件名，内容仅为文件签名及匹配元数据，不含正文；同一来源的缓存跨会话与进程重启复用。缓存采用受限校验与原子替换，变化合并后写入，内容未变不重写。它是可重建的本地加速数据。

运行诊断按监听、缓存读取、索引刷新和缓存保存分别维护当前状态；各自成功时清除对应错误，其余故障继续报告。缓存原子替换成功也会解除启动时的坏缓存告警。诊断变化参与快照发布判断，预览与状态使用当前故障而非历史失败。准备阶段只更新进度并保留上次完整诊断的故障标记；根层与深层汇总后的完整诊断才更新告警去重状态，持续故障只通知一次，完整确认恢复后再次发生可重新通知。终止性加载失败即时报告。启动后台报告携带会话生命周期和快照版本，仅在仍对应当前状态时发布成功或失败结果。显式配置命令在完成当前完整诊断后返回；消息发送仍只在准备阶段更新进度。

`before_agent_start` 只更新根层默认笔记并选定来源。首次 `context` 在用户消息已显示后发布本轮索引快照；初次后台构建尚未完成时等它就绪，保证当轮命中不遗漏。状态栏显示准备状态。同一轮工具后续调用复用该快照；原始消息完成后在内存中匹配一次完整文本，覆盖跨流式片段的关键词。已移除或停用的来源及笔记从后续候选中移除；监听不可用时报告原因，改用 30 秒后台检查。

关键词提醒与默认注入共用笔记注入预算，默认内容优先；提醒按稳定顺序加入，保留完整单篇入口，超限明确报告。frontmatter 上限、来源信任和发现边界沿用现有规则。每个来源的索引与解析缓存各设 16 MiB 序列化估算上限，合并后的提醒候选也限制为 16 MiB；单次来源扫描最多处理 100,000 个深层条目；待提醒候选按路径合并，受当前索引范围约束。无关键词与解析失败的文件也缓存结果，避免每轮重读；索引诊断最多展开 100 项，其余报告数量。实际库与合成样本的成本见验证记录。

## 验证重点

### 首版

- 检查实际发给模型的完整上下文，确认全文、description、定位、文件路径与文件夹入口正确，原有提示保留且没有重复注入；定位在全文和摘要模式下均可见，未配置时自然省略。
- 在真实 Pi 会话中，完成从文件夹入口发现并读取一篇深层笔记的操作。
- 从实际配置入口设置目录，并核验新增、修改、删除笔记以及切换目录后的下一轮行为。
- 实跑错误 frontmatter、字符串形式的 `"false"`、目录不可读等异常输入，确认报告清楚且不会意外注入正文。

### .note 来源

- 在受信任与未受信任项目中分别实跑：未信任时不注入内容且预览说明原因。
- 核验嵌套 `.note`、与全局目录重叠、git 根边界，以及来源间同名笔记的注入版式。
- 用超出预算的真实规模来源验证：整份来源排除并明确报告，其余来源保持完整。

### 关键词提醒

- 在真实 Pi 请求中核验：用户输入命中当次提供；模型思考和回复命中在下次正常调用提供；最终回复命中不会额外续跑。思考触发的覆盖结论限定在实测渠道实际提供的文本。
- 核验全局目录和 `.note` 深层笔记均可命中，根层已展示笔记和重复命中不追加提醒；直接检查完整请求中的提醒内容，再验证 AI 能按路径读取全文。去重断言识别结构化提醒消息，工具读取结果中出现相同 description 不算重复注入。
- 核验流式跨片段命中、连续工具轮次、会话恢复、分支切换和上下文压缩；笔记已移出上下文后只在新的命中时重新提供。
- 实跑错误类型与空白关键词、错误 frontmatter、隐藏条目、符号链接和未受信任 `.note`，确认无意外匹配或越界注入；工具输出和插件注入内容中的关键词也不触发提醒。
- 用真实规模笔记库分别检查首次后台准备、重启缓存校验、未变化发送和单文件变更；验证损坏缓存、离线变更、删除/重命名、来源替换与撤销信任。记录实际 TUI 显示延迟，并区分模型响应等待。连续流式匹配和预算超限另行验证；记录时间、内存、文件读取量及被排除项目。具体已验证场景、环境与证据以验证记录为准。
