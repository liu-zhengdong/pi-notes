# 验证记录

## 两阶段诊断的告警去重

准备阶段只更新状态，完整诊断才更新告警去重；过期的启动后台结果不再发布。新增 18 项扩展事件回归：关键词、监听、缓存及根层/深层混合故障从启动到连续两次发送各只通知一次；确认恢复后重现可再通知，终止性加载失败即时报告且去重；延迟的启动成功/失败不会覆盖修复、停用、无效配置或关闭会话后的状态；参数和菜单中的设置/停用命令会完整发布当前诊断，正确提示新错误并清除恢复标记。

直接审阅了捕获的实际通知文本与状态参数。测试运行真实扩展处理函数、根层扫描和通知格式化，深层索引发布用可控故障替身隔离监听时序，不是完整 TUI 测试。最初 6 项回归在修改前 5 项失败；新增的 8 项竞态回归中，6 项在缺少快照版本校验的中间实现上失败，关闭会话的 2 项原已通过；4 项命令诊断回归在补齐命令路径前全部失败，最终全部通过。`npm run check`、`npm test`（78 项、无跳过）和 `npm run test:keywords`（18 个本地模型端请求）通过。本次未重测 TUI 渲染或延迟。

原始失败/成功输出与前后源码证据：`.artifacts/diagnostics-dedup-20260916/raw/`，反向证据为 `red-notifications.log`、`race-red-corrected.log` 和 `commands-red.log`，最终全套验证为 `delivery-*.log`；宿主记录：`.artifacts/keywords-1789550703061/raw/`。以下保留各轮历史验证的范围。

## 索引诊断恢复

代码健康评审后的修复：四类故障独立保存，成功时清除各自错误；坏缓存被原子替换后解除读取告警。`npm run check`、`npm test`（60 项，无跳过）与 `npm run test:keywords`（18 个本地模型端请求）通过。

新增三项恢复测试，直接审阅实际发布的诊断：监听和索引同时失败 → 监听恢复仅保留索引错误 → 索引恢复清空；坏缓存与写入失败同时报告，成功刷新不掩盖磁盘问题，缓存修复后清空；无需重写的已持久化内容也能解除保存失败。独立工作副本删除六处成功清理语句，三项回归测试全部失败，证明能检出旧告警残留。

原始输出与源码哈希：`.artifacts/health-recovery-20260916/raw/`；宿主记录：`.artifacts/keywords-1789549549615/raw/`。本次未重测 TUI 或延迟；以下性能数据保留上一轮实现的测量边界。

## 首条消息延迟与缓存复用

已将深层关键词扫描移出发送前处理：会话打开时后台准备，文件变化时增量更新，跨会话与重启复用磁盘元数据。未变化的发送不遍历深层目录、不重建匹配表。环境为 Pi 0.85.1、Node.js 26.1.0、macOS / arm64。

### 实际 TUI 对照

使用用户已配置的全部扩展，在独立 tmux 会话中运行当前安装的 Pi CLI，临时进程内插桩，没有改动宿主源码或用户全局配置。两次真实请求使用已有 `openai-codex/gpt-6-astra` 认证、low thinking，禁用工具并只要求回复 OK；分别在打开后约 6 秒和重启后约 4 秒发送。

| 场景 | notes 发送前处理 | 用户消息显示处理 | 随后的终端写入 |
| --- | ---: | ---: | ---: |
| 修改前，首次 | 1,996 ms | 2,002 ms | 2,025 ms |
| 修改后，首次 | 0.69 ms | 4.91 ms | 34.50 ms |
| 修改后，重启复用缓存 | 0.80 ms | 6.36 ms | 42.50 ms |

修改后两次 notes 的首个 context 处理分别为 0.27 ms、0.33 ms。直接审阅实际 TUI 字符网格，均显示用户消息、OK 和正常 notes 状态。终端写入不是终端软件实际呈现像素的时间，也不是模型首字等待时间。

原始对照：`.artifacts/submit-latency-20260916/raw/`；当前实现：`.artifacts/submit-fixed-1789548523/raw/`，包括计时、实际屏幕文本和源码哈希。两次结束后关闭了测试会话。

### 真实规模

原样执行 `npm run bench:store -- /Users/liuzhengdong/Obsidian笔记` 和 `npm run bench:store`。每个样本冷启动一次、磁盘缓存重启一次，各测 5 次未变化发送。发送指标含根层扫描、来源选择和发布本轮索引，不含宿主其他扩展、渲染和模型等待。

| 样本 | 首次后台准备 | 磁盘缓存加载与校验 | 缓存重启重新读取 | 未变化发送范围 |
| --- | ---: | ---: | ---: | ---: |
| 实际库，7,081 篇深层笔记，无关键词候选 | 2,029.52 ms | 238.74 ms | 0 篇 | 0.19～0.37 ms |
| 合成 10,000 篇，2,500 篇有关键词 | 1,103.41 ms | 208.95 ms | 0 篇 | 0.06～0.31 ms |

两种规模的未变化发送均复用同一个已发布 Map，没有增加扫描次数。合成样本冷准备期间还收到一批文件事件并做了增量校验；结果中的 reads/cacheHits 是各来源最后一次刷新读数，不是累计 I/O。单文件编辑测试确认只读取受影响文件，不刷新其余 128 篇未变化文件。

实际库缓存为 1,329,381 字节，合成缓存为 2,330,399 字节；变化合并后原子保存，未变化的重启不会重写缓存。进程 RSS 分别为 147.2、166.6 MiB，包含 Node、依赖和样本准备，不代表插件增量内存。原始结果：`.artifacts/store-1789548727784/raw/`、`.artifacts/store-1789548730274/raw/`。

### 验证与边界

- 类型检查、57 项单元测试、18 请求关键词宿主集成、8 请求原有集成以及 TUI 回归全部通过。现有集成覆盖真实宿主的新会话、恢复、分支和压缩，且直接审阅了当前实现的完整笔记块和用户当次关键词提醒，确认只有摘要与路径，没有正文泄露。
- 反向实跑：损坏 JSON、错误版本/来源、越界/隐藏/相对路径、伪造正文和非法关键词缓存被拒绝；缓存符号链接不读取也不改写目标文件。另验证离线修改、增删改与重命名、目录替换、后代目录符号链接、撤销信任、元数据损坏后修复，以及缓存不可写时仍能使用内存索引。
- 首次后台准备未完成时，用户消息先显示，首次模型请求仍等索引就绪，避免漏掉提醒；测试确认 prepare 立即返回和首轮完整提醒。上面的真实 TUI 样本均在后台准备后发送，不代表“零等待打开并立即发送”的模型请求延迟。
- 监听失效降级为 30 秒后台检查；该定时恢复分支以及 Windows/Linux、网络盘和同步软件丢事件的行为尚未专门实测。本次没有修改用户笔记。仅新加载的扩展实例使用新实现，现有会话需要 `/reload` 或重启。

该轮自动化证据：`.artifacts/keywords-1789548483801/raw/`、`.artifacts/integration-1789548487723/raw/`、`.artifacts/tui-1789548488994/raw/`。运行时源码与各自源哈希一致；原始证据清单见 `.artifacts/store-delivery.json`。其后诊断恢复修复的证据单独列于本文开头。

## 关键词提醒功能验收（历史）

已实现并验证：全局与受信任 `.note` 深层笔记按关键词提供摘要入口；根层和仍在上下文中的提醒去重；用户输入当次生效，模型回复/可见思考留到下一次正常调用，不额外续跑。环境为 Pi 0.85.1、Node.js 26.1.0、macOS / arm64。

- **自动化**：`npm run check`、50 项单元测试、`npm run test:keywords` 的 18 个真实 Pi/本地模型端请求、原有 8 请求集成回归及 TUI 回归均通过。
- **真实模型**：`npm run test:live:keywords` 使用已有 `openai-codex/gpt-6-astra` OAuth 认证的隔离临时副本、low thinking，实际 3 个请求完成提醒→`read`→回答，以及最终回复命中后的下一次输入提醒。上限为 8 请求，每次用户任务等待 150 秒；认证副本退出时删除。
- **直接审阅**：查看了真实出站提醒、工具读取结果、模型答案，确认首个请求只有概要与路径、没有验收口令；不是仅依据测试结果判断可用。关键词提醒使用隐藏上下文消息，没有增加需要操作的新界面。

尚未覆盖其他 Pi 版本、其他操作系统、同步/网络盘、与其他上下文改写扩展的组合。思考触发实测来自本地 SSE `reasoning_content` 经真实 Pi 转成思考消息；不能据此保证所有模型渠道都暴露思考。上下文压缩使用真实宿主生命周期和确定性测试摘要，未评估模型生成摘要的质量。

### 自动化与反向输入

本地模型端核验用户当次命中、思考/回复跨流式片段命中、连续工具请求保留旧提醒、最终回复不续跑、全局与 `.note`、根层去重、重启、压缩后保留/移除的提醒、新命中重现、分支隔离、预算恢复、停用来源和撤销信任。另在实际 Pi 中定位到触发助手消息本身，再继续；在请求途中 `SIGKILL` 宿主后重启，确认未落盘提醒仍待提供；另在工具轮次已保存提醒、`agent_end` 尚未记录完成时终止宿主，恢复后压缩，确认已移除的提醒不会凭旧命中重现。

反向输入实跑拒绝：`keywords` 标量/null/数字/映射、混合列表、空字符串和空白字符串；错误 YAML、别名、自定义标签、超长 frontmatter；隐藏文件/目录、符号链接、不可读文件和未受信任 `.note`。工具结果、工具参数、注入内容和标记为 redacted 的思考不触发提醒。110 篇损坏元数据样本确认诊断有界、下一次读取命中负缓存、修复后恢复。

原始记录：

- 关键词宿主集成：`.artifacts/keywords-1789521850631/raw/`。
- 原有集成回归：`.artifacts/integration-1789521847726/raw/`。
- TUI 回归：`.artifacts/tui-1789521849237/raw/`。
- 真实模型：`.artifacts/live-keywords-1789521847722/raw/`；实际答案为 `BRAVO-927`，唯一工具调用为按提醒路径 `read`。

去重验证按结构化提醒消息计数；全文读取结果会合法地包含 frontmatter 中同一段 description。早期真实模型记录 `.artifacts/live-keywords-1789521012703/raw/` 因全请求字符串计数误报重复而失败，原始证据保留；修正断言后重新执行同一入口通过。

### 规模与成本

执行 `npm run bench:keywords -- /Users/liuzhengdong/Obsidian笔记`，只读实际库，并创建独立合成样本。本次单轮观测：

| 样本 | 冷扫描 | 未变化刷新 | 刷新读取文件 | 有关键词的深层笔记 |
| --- | --- | --- | --- | --- |
| 实际库，7,081 个深层 Markdown 文件 | 2,307.62 ms | 149.34 ms | 0 | 0 |
| 合成 10,000 篇，其中 2,500 篇有关键词 | 1,330.69 ms | 155.16 ms | 0 | 2,500 |

合成样本匹配 100 条各 36,881 字符的消息共 3,036.48 ms，约 30.36 ms/条；匹配阶段无文件读取，每条原始消息只匹配一次。实际库有 1 项元数据问题；未修改原始笔记。进程 RSS 最高观测 154.3 MiB，包含 Node、依赖、实际库缓存和合成样本生成，不能当作插件增量内存或上限承诺。

关键词索引与元数据缓存各限制为 16 MiB 序列化估算值，每次深层扫描最多 100,000 个条目；这不是 JavaScript 进程 RSS 上限。完整结果：`.artifacts/keyword-benchmark-1789521081103/raw/results.json`。这些是本机样本，不是所有库和存储介质的延迟保证。

## .note 项目来源验证（历史）

多来源模型实现后的补充验证。环境同首版（Pi 0.85.1、Node.js 26.1.0、macOS / arm64）。

### 自动化

- `npm run check`：通过。`npm test`：27 项通过，无跳过。
- 新增覆盖：发现顺序（浅→深、git 根边界）、与全局目录重叠去重、未信任来源标记、注入顺序（全局在前、项目浅→深）、预算整源排除、不可读来源跳过且其余照常。
- 原有 19 项扫描/缓存/预算/配置测试迁移到新 Snapshot 结构；集成与 TUI 测试同步适配。

### 真实实跑（隔离 agent 目录 + 本地桩模型端记录出站请求）

- 未信任仓库（含 `.pi/settings.json`、无保存决定）：`/notes preview` 与出站上下文均不含项目笔记，仅列出未注入来源及原因。
- 已信任仓库（`trust.json`）：`# 项目笔记` 块注入全文与摘要，深层文件不出现。
- 真实 prompt 出站捕获：全局块在前、项目块在后；全文、摘要、定位与完整路径均在，嵌套正文、未信任原因与深层文件不泄漏。
- 预算 1 KiB：超预算的项目来源整体排除并明确报告，全局来源照常完整注入；单个超大笔记按单篇规则降级为未展开，不阻断同源其他笔记。
- 实测确认的 Pi 行为：`--no-approve` 本身即 trust override（强制关闭信任）；不含 trust-requiring 资源的目录自动受信。已写入 DESIGN 信任门槛描述。

### 集成回归

`npm run test:integration` 在新结构下通过（8 个本地请求），预览与出站 system 逐字一致、无历史污染等原有断言不变。

## 首版结论与范围（历史）

已跑通：安装本地 Pi package、配置目录、预览与默认注入、修改后的刷新，以及 AI 从文件夹入口逐层找到并读取深层笔记。

验证环境：Pi 0.85.1、Node.js 26.1.0、macOS / arm64。测试隔离了 Pi 配置，没有把本插件安装到用户的真实全局配置，也没有迁移现有 USER。

自动化检查与直接审阅分开进行：下面的测试通过后，还直接查看了完整出站 system/instructions 文本、模型工具调用与回答，以及真实终端渲染的字符网格。

尚未验证：Windows / Linux、其他 Pi 版本、长会话压缩与恢复/分支、与其他提示词改写插件的组合。zg 本身、Obsidian 应用内渲染和用户的真实笔记库不在此次测试范围内。

## 自动化验证

### 类型与单元测试

- `npm run check`：通过 TypeScript 检查。
- `npm test`：22 项通过，无跳过。
- LSP situational tools 在本会话无法激活，因此类型检查使用实际运行的 `tsc --noEmit`，未声称获得 LSP 诊断。

覆盖属性解析、BOM/CRLF、多行 purpose、无属性笔记、两种注入模式、原文保留、根层发现、缓存更新、原子替换、创建/删除、切换目录、文件权限、配置持久化、精确 UTF-8 预算边界、终端换行与滚动。

反向输入实跑的拒绝清单：

- 带引号的 `"false"` / `"true"`、数字形式的 defaultopen。
- 重复字段、损坏 YAML、非映射 frontmatter、非文本 description / purpose。
- YAML 别名引用、自定义标签、缺少 frontmatter 结束分隔符。
- 超长 frontmatter、全文文件超限、来源超预算、预算差一个字节。
- 不可读文件/目录与不可读/不存在的来源、损坏 JSON、相对配置路径、非法预算与未知配置字段。

另确认隐藏文件、符号链接、非 Markdown 文件、深层 `defaultopen: true` 不会进入默认笔记正文。被拒笔记保留路径与原因；失败后没有复用旧正文。

### 真实 Pi + 本地模型端

`npm run test:integration` 在隔离配置中原样执行 `pi install .`，随后启动真正的 Pi RPC 进程。最终一次使用 8 个本地 HTTP 模型请求，检查：

- package 自动发现 `/notes`；空格/中文路径设置、错误路径不覆盖有效配置。
- 预览笔记块与真实 HTTP 请求中的 system 文本逐字一致。
- 两种模式都有 purpose，原系统提示保留，连续两轮不重复注入。
- 编辑、新建、删除和属性损坏在下一轮生效。
- `/new` 对应的 RPC 新会话、切换目录、停用、损坏配置与错误提示。
- 预览和默认笔记块不作为独立消息写进对话历史。
- print / JSON 模式下，`/notes preview` 只向 stderr 输出笔记，stdout 保持空白或合法 JSONL；无需发起模型请求。

最终记录：`.artifacts/integration-1789463099909/raw/`。

## 真实模型与实际产物审阅

### 模型按需读取

`npm run test:live` 使用已有 `openai-codex/gpt-6-astra`、OAuth 认证的临时副本、low thinking。每次测试限制最多 8 个请求，单次任务等待 150 秒；临时认证文件在退出时删除。

最终样本实际产生 4 个模型请求。模型执行：

1. `ls` 项目目录。
2. `ls` Alpha 子目录。
3. `read` 读取 `验收.md`。
4. 回答口令 `ALPHA-731` 及来源。

直接检查首次出站完整 instructions，确认根 USER 正文、两篇笔记的 purpose、摘要和文件夹路径都正确；其中没有深层验收口令。口令是在工具读取后才出现的，不是预先泄露在默认上下文中。

最终记录：`.artifacts/live-1789463004650/raw/`，包含真实请求、RPC 事件、模型信息和回答。实验前后的 `src/` 哈希一致。

### 终端界面

`npm run test:tui` 在独立 tmux server 内驱动真实 Pi。直接审阅 100×40 与 44×24 两种终端尺寸的渲染字符网格；这是实际 TUI 输出，不是静态模拟界面，也不是操作系统窗口截图。

实测设置目录、选择预览、翻页、窄屏、返回、单次 Enter 执行完整命令、错误路径、损坏笔记、停用和目录失效后的恢复入口，共保存 12 组文本与 ANSI 屏幕记录。

对照初版与修正版：

- 精确命令不再出现重复补全项，第一次 Enter 直接执行。
- 窄屏中优先保留返回与滚动提示；完整路径和正文可逐页查看。
- 目录失效明确显示“配置待检查”，并保留重新设置入口。

最终记录：`.artifacts/tui-1789463004635/raw/`。先前失败的界面记录留在各自的 `.artifacts/tui-*` 目录；自动化驱动的启动/按键时序问题与插件修正的区别见 [设计演进](../DESIGN-Evolution.md)。

## 首版规模与成本（历史）

`npm run bench` 使用合成文件系统样本：1,000 篇根笔记，每篇正文 8 KiB，另有 10,000 篇深层笔记；使用默认 256 KiB 注入预算。

| 扫描 | 耗时 | 读取根笔记文件 | 缓存命中 |
| --- | --- | --- | --- |
| 冷扫描 | 262.78 ms | 1,000 | 0 |
| 未变化的下一次扫描 | 19.40 ms | 0 | 1,000 |
| 修改一篇后 | 17.91 ms | 1 | 999 |

深层笔记未被扫描，默认输出只提供一个 archive 文件夹入口。首次笔记块为 179,307 字节。进程 RSS 观测为 118.8 MiB，包含 Node、依赖、样本创建等，不能解释为插件增量内存。

这是本机合成样本的一轮观测，不代表网络盘、同步盘或所有库规模的延迟保证。记录：`.artifacts/performance/benchmark-final-1789463004.txt`。

## 证据与复现

运行入口见 [README](../README.md#开发与验证)。每次集成、TUI 和真实模型测试都生成独立目录，`raw/` 与可修改的 `work/` 分开。原始数据由测试程序写入，不经过源文件格式化流程；请求文件只保存 payload，不保存认证头。

交付前对最终原始记录生成 SHA-256 清单并复核；旧版本记录保留自己的源码哈希，不以新哈希替代旧版本证据。关键词交付清单为 `.artifacts/keyword-delivery-1789521970.json`：81 个原始文件复核一致，3 份当前源码记录与交付代码一致；基准和早期失败样本明确标为历史记录。`.artifacts/` 不进入 package。
