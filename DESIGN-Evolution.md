# Pi Notes 设计演进

## 2026-09-20 · 未配置时默认使用 agent 目录下的 notes/

- 发生：用户确认全局笔记未配置时应跟随 AgentDir，默认 `<agentDir>/notes`，与 Experience 的 run-archive 一致，便于 Atrium 按身份隔离。
- 分析：`notes.json` 与缓存本来就在 agent 目录；会串用的是文件里的绝对 `directory`。`/notes clear` 写入 `null` 必须仍表示显式停用。默认目录不存在时若仍写入该路径，每轮会报 ENOENT。
- 改变：没有 `notes.json` 或没有 `directory` 字段时，若 `<agentDir>/notes` 存在且可读则用作全局来源，否则不加全局来源、不报错、不自动建目录。已有绝对路径配置不变。

## 2026-09-16 · 索引准备与缓存复用

- 发生：用户报告首条消息发送后 TUI 等待约 1～2 秒。真实宿主对照确认，发送前深层冷扫描耗时约 2 秒；同会话仍逐次遍历全库检查文件。用户要求会话打开时准备，已有缓存跨会话复用。
- 分析：文件元数据的有效期与对话生命周期无关。只把扫描移到启动会阻塞打开，只有内存缓存也无法覆盖宿主在切换会话时重建扩展实例的行为。采用按来源保存的元数据缓存，加启动后台校验和文件监听；离线变化仍需重启时校验，不能直接信任旧缓存。相比引入数据库，受限 JSON 缓存更直接，代价是变化合并后原子重写该来源缓存，而不是逐项写入；未变化不写。
- 改变：新增 `KeywordStore`，将递归工作移出 `before_agent_start`，监听变化并增量更新；首个 `context` 才发布本轮快照。首次索引未就绪时先显示用户消息再等待，避免以漏提醒换速度；状态栏显示准备中。缓存损坏则重建，缓存不可写保留内存能力，监听失效明确告警并后台定期校验。测试涵盖缓存复用、坏缓存与路径/正文伪造、离线编辑、目录替换和信任撤销；真实规模与 TUI 结果见验证记录。

## 增加笔记定位

- 发生：用户计划将 USER 的用户建模功能放入笔记目录，提出增加“定位”字段，说明笔记对 AI 的价值和使用要求。
- 分析：description 主要帮助判断笔记是否相关；定位帮助 AI 理解如何使用这些内容。只在摘要模式展示定位，会让默认全文展开的 USER 笔记缺少这层说明。
- 改变：在 frontmatter 中增加可选自由文本字段 `定位`，在全文和摘要模式下均展示。沿用统一笔记机制，由笔记自身表达作用。此次仅更新设计，现有 USER.md 及其注入方式保持不变。

## 定位字段采用英文名

用户要求定位字段使用英文名；选用表达用途与价值的 `purpose`。已更新设计中的字段定义与 frontmatter 示例，定位的语义和注入行为保持不变。

## 首版实现

- 发生：用户确认设计并授权开发。
- 分析：所需闭环是配置一次、自动提供根入口、按需深入；现有 Pi 文件工具已能承担后续读取，无需新建搜索索引或笔记工具。自动注入需要明确资源上限，避免大笔记或错误属性导致上下文意外膨胀。
- 改变：实现全局 `notes.json`、`/notes` 配置与只读预览、根层发现、严格属性解析、变更缓存和逐轮系统提示追加。增加字节预算、单篇失败保留路径和整块失败明确报告；原 USER 注入机制保持不变。

## 真实终端验证中的修正

- 发生：真实 Pi TUI 中，完整输入 `/notes preview` 后第一次 Enter 被同名补全项消耗；44 列预览将返回提示截掉。目录失效后配置入口的标题也容易误表述为尚未配置。
- 分析：组件宽度测试和 RPC 命令测试没有覆盖键盘补全行为；完整路径预览需要滚动，返回操作必须在窄屏可见。自动化驱动另有过早输入、Escape 与后续字符合并为 Alt 键的问题，属于驱动时序而非插件故障。
- 改变：精确匹配时不再返回重复补全项；预览提示优先返回和滚动；配置失效显示“配置待检查”并保留重新设置入口。TUI 验证等待宿主就绪并分隔 Escape 与后续输入，以原样单次 Enter 重跑命令。

## .note 项目来源设计

- 发生：用户提出在各个路径识别 `.note` 目录并动态注入，讨论后选定方案 A：按会话位置向上发现，先不做触达即时注入。
- 分析：发现时机决定整体形态；A 对齐 Pi 既有的 cwd 向上项目发现规则，且扩展 API 提供 `ctx.isProjectTrusted()`，可复用项目信任边界防止第三方仓库内容自动进入模型上下文；B 的会话中频化注入和缓存失效成本推迟到后续需要时再考虑。
- 改变：DESIGN.md 新增「项目笔记目录（.note）」与对应验证重点，标注已确认待实现；本次仅更新设计，未动实现。

## .note 项目来源实现

- 发生：用户确认设计并授权实现；随后实跑验证。
- 分析：核心改动是把「单一目录快照」模型改为「有序多来源」模型——发现（cwd 向上到 git 根）、策略（全局优先、项目浅到深、全局目录覆盖的路径去重）、信任（`ctx.isProjectTrusted()`，实测发现 `--no-approve` 本身就是 trust override 会强制关闭信任；无 `.pi` 等资源的普通项目在 Pi 中自动受信，与 AGENTS.md 同层）、预算改为整源排除而非整体拒绝。实测还发现无资源项目的自动受信行为，已补充进信任门槛描述。
- 改变：实现 `discoverNoteDirectories` / `resolveSources` / 按来源加载与排除、`# 项目笔记` 双标题版式、preview 附「未注入来源」清单；`/notes` 状态、摘要与预览聚合多来源。新增发现、去重、信任、顺序、预算、不可读来源六类测试；integration 与 TUI/基准适配新 Snapshot 结构。实测通过：未信任（`.pi/settings.json` 未决定）不注入且报原因、已信任注入、全局→项目顺序、深层不注入、单笔记超限降级、整源超预算排除且其余来源完整。

## 2026-09-16 · 关键词按需提醒设计

- 发生：用户提出用 `keywords` 匹配用户输入、模型思考和回复，主动提供笔记概要供 AI 按需读取；确认只在下一次正常模型调用时提供，不额外续跑，并明确全局与 `.note` 子目录均在范围内、根层已展示笔记无需重复注入。
- 分析：根层已有默认入口，关键词提醒的主要价值是发现深层笔记。复用 `description` 比生成摘要更直接；思考与回复中的命中无法改变当前正在生成的请求。去重需要依据当前有效上下文，永久会话名单会在压缩或分支切换后漏掉已不在上下文的笔记。
- 改变：DESIGN.md 新增关键词属性、递归来源范围、下一次正常调用提醒、上下文去重、资源边界及待验证场景；同时将已有 `.note` 实现状态从过时的“待实现”更新为“已实现”。本次仅修改设计，关键词功能尚未实现或实测。

## 2026-09-16 · 关键词实现与恢复边界

- 发生：用户授权实现。真实宿主验收与审阅发现：运行中的工具轮次可能看不到刚入队的自定义消息；分支可停在助手消息与其命中记录之间；提醒入队不等于已持久化。
- 分析：永久已见名单和单纯的待提醒集合都不能覆盖这些生命周期。需要区分当前请求、真实会话消息和分支内待提醒增量，并只恢复末尾尚未检查的原始触发消息。
- 改变：增加关键词元数据索引、完成消息匹配、上下文去重和共享预算；使用小型来源检查点、投递 ID 与 `agent_end` 持久化确认。轮内轻量副本覆盖工具快照差异；恢复按时间顺序同时对账实际投递消息，覆盖消息已落盘但完成记录尚未写入后被压缩的边界。真实 Pi 的分支定位、落盘前后分别终止并恢复、压缩和连续工具轮次均补入验收。代价为每条原始用户/模型消息一条小型检查点，换取分支精确恢复；不重写全量索引或会话快照。
- 验证方法修正：真实模型按提醒读取全文后，工具结果会含同一 description。原有全请求字符串计数误报重复，现改为识别结构化提醒消息，并保留失败原始记录及重新执行结果。当前验证范围与性能观测见 `docs/verification.md`。

## 2026-09-16 · 索引诊断按故障归属恢复

- 发生：代码健康评审指出，监听、缓存读取、索引构建和缓存写入共用一个错误字段，既相互覆盖，也在恢复后残留告警；实跑故障与恢复序列确认需要按子系统分别维护当前状态。
- 分析与改变：拆分四类诊断，对应成功路径独立清除；原子替换修复坏缓存时同时解除读取告警，已持久化内容无需重写也可解除保存失败。设计补充诊断生命周期。新增三项恢复测试，保留并直接审阅各阶段实际诊断；删除恢复逻辑的工作副本被三项测试全部拒绝。类型检查、60 项单元测试与 18 请求宿主集成通过，未重测 TUI 延迟。

## 2026-09-16 · 完整诊断驱动告警去重

- 发生：代码健康评审指出根层准备阶段会清空深层告警的去重状态，持续故障每轮重复通知；新增事件回归在修复前检出重复通知及状态标记提前消失。
- 分析与改变：局部快照不代表故障已恢复。将准备状态更新与完整诊断发布分开，仅后者推进告警去重；准备中保留上次故障标记。补充连续两次发送、根层与深层合并、恢复后重现和终止性加载失败的事件测试。设计明确完整诊断的发布责任。独立审阅进一步指出启动后台结果可能晚于新快照；增加快照版本校验，并用成功/失败晚到的回归覆盖修复、停用、无效配置和关闭会话。随后核对全部调用点，补齐参数与菜单的设置/停用命令完整诊断，避免只更新准备状态导致漏报或保留已恢复标记；对应四项回归先失败再通过。最终 78 项单元测试及 18 请求宿主集成通过；消息发送路径未新增扫描或等待，显式配置命令等完整诊断后返回。
